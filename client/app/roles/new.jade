div(ng-include='"components/navbar/navbar.html"')
.container
  form(name='newRoleForm', ng-submit='saveRole()', novalidate="")    
    md-list(ng-cloak)
      md-list-item
        md-input-container.md-block.no-bottom-margin(flex="")
          label Name
          input(type='text', name='name', ng-model='role.name', required="")
          div(ng-messages="newRoleForm.name.$error")
            div(ng-message="required")  This is required.
      md-list-item
        p Is this role an instructor?
        md-switch(class="secondary", ng-model="isInstructor")
      md-subheader(class="md-no-sticky")
        | Activities
      md-list-item(ng-repeat="activity in activities")
        p {{activity.name}}
        md-checkbox(class="md-secondary", ng-model="activity.selected")
    md-button.md-primary.md-raised(type='submit')
      | Save Role
